name: Go

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.15

      # Build
      - name: Build
        run: go build -v ./cmd/shrinker/main.go

      # Check Go code format
      - name: Lint
        run: "! gofmt -l . 2>&1 | read"

      # Check Go code sanity
      - name: Vet
        run: go vet ./...

      # Run Go unit tests
      - name: Test
        run: go test -v ./...
